# Build output
dist/
cli/target/
# A manual glib-compile-schemas run in the tree. Nothing in the repo writes it
# here: build.sh compiles the installed copy, start-headless.sh its scratch
# copy, and the zip excludes it because every install path compiles the XML.
window-control@carlo9890.github.io/schemas/gschemas.compiled

# IDE
.idea/
.coder/

# OS
.DS_Store

# Debug output
output/

# Nested-session logs, one per run (scripts/start-nested.sh)
nested-*.log
